import * as FarmingHooks from "./hooks";
import * as FarmingComponents from "./components";
import * as FarmingTypes from "./types";
import * as FarmingUtils from "./utils";

const FarmingModule = {
    hooks: FarmingHooks,
    components: FarmingComponents,
    types: FarmingTypes,
    utils: FarmingUtils,
};

export default FarmingModule;
